## Summary

<!-- What changes and why, in a sentence or two. -->

## Changes

<!-- Bullet the notable changes. Call out anything that touches config, secrets, or the deploy shape. -->

-

## Test plan

<!-- How this was verified. `uv run pytest` is the baseline; note anything exercised manually. -->

- [ ] `uv run ruff format --check` && `uv run ruff check`
- [ ] `uv run pytest` (coverage stays ≥ 90%)
